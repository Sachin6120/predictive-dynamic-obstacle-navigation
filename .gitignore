build/
install/
log/
*.pyc
__pycache__/

# Disposable scratch output from manual validation runs (raw launch logs,
# ad-hoc inspection scripts, intermediate JSON). Curated, reproducible
# validation evidence belongs in validation/ instead, which stays tracked.
validation_logs/
